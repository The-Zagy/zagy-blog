{
  "name": "@acme/constants",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "lint": "eslint",
    "build": "tsup index.ts --format esm --dts",
    "dev": "echo \"WATCHING CONSTANTS\" && pnpm build --watch",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "eslint": "^8.32.0",
    "typescript": "^4.9.4"
  }
}